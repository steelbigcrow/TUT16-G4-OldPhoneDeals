# React 前端 vs Spring Boot 后端兼容情况

检查日期：2026-01-30  
检查范围：`react-frontend/`（Vite + React + TypeScript）与 `spring-old-phone-deals/`（Spring Boot + MongoDB + JWT）

## 1. 结论（总体）

结论：**当前 React 前端与 Spring Boot 后端在路由前缀、请求方法、鉴权方式、返回结构上总体兼容，可直接联调。**

核心依据：
- 前端 `axios` 统一以 `/api` 为 `baseURL`（见 `react-frontend/src/api/client.ts`），后端所有业务接口也以 `/api/**` 暴露（见 `spring-old-phone-deals/src/main/java/com/oldphonedeals/controller/*`）。
- Vite 开发代理已将 `/api`、`/images`、`/uploads` 转发到 `http://localhost:8080`（见 `react-frontend/vite.config.ts`），与 Spring Boot 默认端口一致（见 `spring-old-phone-deals/src/main/resources/application.yml`）。
- 双端统一使用 `ApiResponse<T>`：`{ success, message?, data? }`（前端类型 `react-frontend/src/types/api.ts`；后端实现 `spring-old-phone-deals/src/main/java/com/oldphonedeals/dto/response/ApiResponse.java`）。

需要注意的点（不影响现有联调，但建议知晓）：
- **分页参数已统一**：所有分页请求统一使用 1-based `page`（第一页为 `page=1`）；后端仍兼容旧调用 `page=0` 作为第一页。
- **上传目录已修复**：`file.upload.dir` 作为上传根目录，图片实际落盘到 `{dir}/images/`；后端同时兼容旧的 `{dir}/images/images/` 目录结构，避免历史图片失效。

## 2. 本地联调方式（推荐）

### 2.1 启动 Spring Boot（后端）

在仓库根目录执行：

```bash
cd spring-old-phone-deals
mvn spring-boot:run
```

默认：
- API 服务：`http://localhost:8080`
- MongoDB（dev profile 默认）：`mongodb://localhost:27017/oldphonedeals-dev`（见 `spring-old-phone-deals/src/main/resources/application-dev.yml`）

### 2.2 启动 React（前端）

在仓库根目录执行：

```bash
cd react-frontend
npm install
npm run dev
```

默认：
- 前端：`http://localhost:5173`
- 通过 Vite 代理访问后端（避免 CORS 问题）：见 `react-frontend/vite.config.ts`

## 3. 协议与关键约定

### 3.1 API 前缀与代理

- 前端所有 API 请求默认发往 `/api/...`（由 Vite 代理转发到 `http://localhost:8080`）。
- 静态资源：
  - `/uploads/**`：用于上传图片访问（后端资源映射见 `spring-old-phone-deals/src/main/java/com/oldphonedeals/config/CorsConfig.java`）
  - `/images/**`：用于内置示例图片（后端 classpath 静态资源映射同上）

### 3.2 统一返回体（非常关键）

前端按如下结构解析错误与成功：

```ts
type ApiResponse<T> = { success: boolean; message?: string; data?: T }
```

后端已提供完全一致的 `ApiResponse`，并通过全局异常处理确保大多数错误也返回该结构（见 `spring-old-phone-deals/src/main/java/com/oldphonedeals/exception/GlobalExceptionHandler.java`）。

### 3.3 鉴权（JWT）

- 前端：对请求自动加 `Authorization: Bearer <token>`（见 `react-frontend/src/api/client.ts`）。
  - 普通用户 token：`localStorage.user_auth_token`
  - 管理员 token：`localStorage.admin_auth_token`
- 后端：Spring Security + JWT Filter（见 `spring-old-phone-deals/src/main/java/com/oldphonedeals/security/SecurityConfig.java`）
  - `/api/admin/**`（除 `/api/admin/login` 外）需要 `ROLE_ADMIN`
  - 公开访问：`GET /api/phones/**`、`GET /uploads/**`、`/api/auth/**` 等

> 注意：前端对 `401/403` 会清 token 并重定向到登录页（见 `react-frontend/src/api/client.ts`），后端也会在未授权/无权限时返回 401/403，行为匹配。

## 4. 接口对照（前端调用 vs 后端实现）

下面仅列出 React 前端实际调用到的接口（见 `react-frontend/src/api/*.ts`），并对照 Spring Controller。

### 4.1 Auth（用户认证）

| 功能 | 前端调用 | 后端实现 |
|---|---|---|
| 登录 | `POST /api/auth/login` | `AuthController#login` |
| 注册 | `POST /api/auth/register` | `AuthController#register` |
| 邮箱验证 | `POST /api/auth/verify-email` | `AuthController#verifyEmail` |
| 重发验证邮件 | `POST /api/auth/resend-verification` | `AuthController#resendVerification` |
| 请求重置密码 | `POST /api/auth/request-password-reset` | `AuthController#requestPasswordReset` |
| 校验重置码 | `POST /api/auth/verify-reset-code` | `AuthController#verifyResetCode` |
| 重置密码 | `POST /api/auth/reset-password` | `AuthController#resetPassword` |
| 获取当前用户 | `GET /api/auth/me` | `AuthController#getCurrentUser` |

结论：路径、方法、请求体字段（`email/password/token/code/newPassword` 等）与前端一致。

### 4.2 Profile（用户资料）

| 功能 | 前端调用 | 后端实现 |
|---|---|---|
| 获取资料 | `GET /api/profile` | `ProfileController#getCurrentUserProfile` |
| 更新资料 | `PUT /api/profile` | `ProfileController#updateCurrentUserProfile` |
| 修改密码 | `PUT /api/profile/change-password` | `ProfileController#changeCurrentUserPassword` |

结论：兼容。

### 4.3 Phones（商品）

| 功能 | 前端调用 | 后端实现 |
|---|---|---|
| 商品列表（分页/筛选/排序） | `GET /api/phones?search&brand&maxPrice&sortBy&sortOrder&page&limit` | `PhoneController#getAllPhones` |
| 特殊列表（soldOutSoon/bestSellers） | `GET /api/phones?special=soldOutSoon` | `PhoneController#getAllPhones`（special 分支） |
| 商品详情 | `GET /api/phones/{phoneId}` | `PhoneController#getPhoneById` |
| 卖家商品列表 | `GET /api/phones/by-seller/{sellerId}` | `PhoneController#getPhonesBySeller` |
| 创建商品 | `POST /api/phones` | `PhoneController#createPhone` |
| 卖家启/禁用 | `PUT /api/phones/{phoneId}/disable`（body: `{ isDisabled }`） | `PhoneController#togglePhoneDisabled` |
| 卖家删除 | `DELETE /api/phones/{phoneId}` | `PhoneController#deletePhone` |

关键字段一致性：
- `brand`：前端使用大写枚举（`SAMSUNG/APPLE/...`），后端 `PhoneBrand` 枚举匹配。
- 列表返回 `data` 内结构：`{ phones, currentPage, totalPages, total }`（前端类型 `PhonesListData`），后端 `PhoneServiceImpl#getPhones` 按相同 key 返回。

### 4.4 Reviews（评论）

| 功能 | 前端调用 | 后端实现 |
|---|---|---|
| 获取商品评论（分页） | `GET /api/phones/{phoneId}/reviews?page&limit` | `ReviewController#getReviews` |
| 添加评论 | `POST /api/phones/{phoneId}/reviews`（body: `{ rating, comment }`） | `ReviewController#addReview` |
| 切换评论可见性 | `PATCH /api/phones/{phoneId}/reviews/{reviewId}/visibility`（body: `{ isHidden }`） | `ReviewController#toggleReviewVisibility` |
| 卖家查看收到的评论 | `GET /api/phones/reviews/by-seller` | `ReviewController#getReviewsBySeller` |

结论：兼容（方法与路径完全一致）。

### 4.5 Cart（购物车）

| 功能 | 前端调用 | 后端实现 |
|---|---|---|
| 获取购物车 | `GET /api/cart` | `CartController#getCart` |
| 加入购物车 | `POST /api/cart`（body: `{ phoneId, quantity }`） | `CartController#addToCart` |
| 修改数量 | `PUT /api/cart/{phoneId}`（body: `{ quantity }`） | `CartController#updateCartItem` |
| 删除商品 | `DELETE /api/cart/{phoneId}` | `CartController#removeFromCart` |

结论：兼容。

### 4.6 Wishlist（收藏夹）

| 功能 | 前端调用 | 后端实现 |
|---|---|---|
| 获取收藏夹 | `GET /api/wishlist` | `WishlistController#getUserWishlist` |
| 添加收藏 | `POST /api/wishlist`（body: `{ phoneId }`） | `WishlistController#addToWishlist` |
| 移除收藏 | `DELETE /api/wishlist/{phoneId}` | `WishlistController#removeFromWishlist` |

结论：兼容。

### 4.7 Orders（订单）

| 功能 | 前端调用 | 后端实现 |
|---|---|---|
| 结账下单 | `POST /api/orders/checkout`（body: `{ address: { street, city, state, zip, country } }`） | `OrderController#checkout` |
| 获取当前用户订单（分页） | `GET /api/orders?page&pageSize` | `OrderController#getCurrentUserOrders` |

结论：兼容。返回结构为 `data: { items, pagination }`，与前端 `OrderPageResponse` 匹配。

### 4.8 Upload（图片上传）

| 功能 | 前端调用 | 后端实现 |
|---|---|---|
| 上传图片 | `POST /api/upload/image`（multipart form-data，字段名 `file`） | `FileUploadController#uploadImage` |

结论：兼容。后端返回 `data.fileUrl` 形如 `/uploads/images/<uuid>.<ext>`，前端会直接把该 URL 存入商品 `image` 字段并用于展示。

### 4.9 Admin（后台管理）

| 功能 | 前端调用 | 后端实现 |
|---|---|---|
| 管理员登录 | `POST /api/admin/login` | `AdminController#adminLogin` |
| 资料 | `GET /api/admin/profile` | `AdminController#getAdminProfile` |
| 统计 | `GET /api/admin/stats` | `AdminController#getDashboardStats` |
| 用户列表（分页/过滤） | `GET /api/admin/users?page&pageSize&search&isDisabled` | `AdminController#getAllUsers` |
| 切换用户禁用 | `PUT /api/admin/users/{userId}/toggle-disabled` | `AdminController#toggleUserStatus` |
| 商品列表（分页） | `GET /api/admin/phones?page&pageSize` | `AdminController#getAllPhones` |
| 更新商品 | `PUT /api/admin/phones/{phoneId}` | `AdminController#updatePhone` |
| 切换商品禁用 | `PUT /api/admin/phones/{phoneId}/toggle-disabled` | `AdminController#togglePhoneStatus` |
| 删除商品 | `DELETE /api/admin/phones/{phoneId}` | `AdminController#deletePhone` |
| 评论列表（分页/过滤） | `GET /api/admin/reviews?page&pageSize&visibility&reviewerId&phoneId&search` | `AdminController#getAllReviews` |
| 切换评论可见 | `PUT /api/admin/reviews/{phoneId}/{reviewId}/toggle-visibility` | `AdminController#toggleReviewVisibility` |
| 删除评论 | `DELETE /api/admin/reviews/{phoneId}/{reviewId}` | `AdminController#deleteReview` |
| 订单列表（分页/过滤/排序） | `GET /api/admin/orders?...` | `AdminController#getAllOrders` |
| 订单导出（CSV） | `GET /api/admin/orders/export?...` | `AdminController#exportOrders` |
| 销售统计 | `GET /api/admin/orders/stats` | `AdminController#getSalesStats` |
| 操作日志 | `GET /api/admin/logs?page&pageSize` | `AdminController#getAllLogs` |

结论：兼容（含导出接口）。

## 5. 分页与参数基准（已统一）

为避免联调时“页码错位”，这里单独强调：

### 5.1 公开商品列表（Search 页面）

- 前端：`page` 从 **1** 开始（见 `react-frontend/src/pages/search/SearchPage.tsx`）。
- 后端：`GET /api/phones` 接收 `page`（1-based），内部转换为 Spring Data 的 0-based（见 `PhoneServiceImpl#getPhones`）。
- 返回：`data.currentPage` 为 1-based。

### 5.2 商品评论分页

- 前端：`page` 从 **1** 开始（如 `react-frontend/src/pages/profile/ProfileReviewsPage.tsx`）。
- 后端：`GET /api/phones/{phoneId}/reviews` 以 1-based 计算 `fromIndex = (page - 1) * limit`（见 `ReviewController#getReviews`）。

### 5.3 当前用户订单分页

- 前端：`page` 从 **1** 开始（如 `react-frontend/src/pages/profile/ProfileHomePage.tsx` 固定用 `page=1`）。
- 后端：`GET /api/orders` 接收 1-based `page`，内部 `page - 1` 转换（见 `OrderServiceImpl#getUserOrders(userId, page, pageSize)`）。

### 5.4 管理后台列表分页

管理后台页面也统一使用 1-based `page`：
- 前端：各 Admin 页面统一使用 `pageNumber`（从 1 开始）并传给 `/api/admin/*` 列表接口。
- 后端：AdminController 将请求参数 `page` 转换为 Spring Data 的 0-based page index，并统一返回 1-based 的 `PageResponse.currentPage`（见 `spring-old-phone-deals/src/main/java/com/oldphonedeals/dto/response/PageResponse.java`）。

## 6. 潜在问题与改进建议（可选）

### 6.1 上传目录路径已修复（兼容旧结构）

已按最佳实践修复并做了兼容：
- 上传根目录改为 `file.upload.dir=./uploads`（dev/prod 同理），图片存储到 `{dir}/images/`。
- 为兼容旧配置（曾使用 `./uploads/images`），后端会自动将其识别为“把子目录写进了根目录”，并回退到父目录作为根目录。
- 静态资源映射对 `/uploads/**` 同时增加了 `{dir}/` 与 `{dir}/images/` 两个资源位置，用于兼容历史落盘在 `{dir}/images/images/` 的文件。

### 6.2 生产环境跨域（不使用 Vite Proxy 时）

开发环境下前端通过代理访问后端，浏览器视角是“同源请求”，CORS 基本不触发。

如果部署为“前后端不同域名/端口”：
- 确保 Spring 的 `frontend.url`/`FRONTEND_URL` 配置为实际前端域名（见 `spring-old-phone-deals/src/main/resources/application.yml` 与 `CorsConfig` / `SecurityConfig`）。
- 如果前端不再使用 `/api` 同源路径，则需要在前端引入可配置的 API BaseURL（当前实现为固定 `/api`）。

### 6.3 时间字段序列化格式

前端普遍用 `new Date(value)` 解析时间（见多个页面的 `formatDate`），因此后端应保持 `createdAt/updatedAt` 等字段输出为 ISO-8601 字符串（Spring Boot 默认行为通常满足）。

避免在 Jackson 配置中开启“时间戳数组/数字”输出（否则前端解析会变差）。

## 7. 快速验证清单（人工冒烟）

1) 启动后端、前端后访问 `http://localhost:5173`，在 Search 页面确认能看到手机列表（调用 `GET /api/phones`）。  
2) 登录后访问 Profile：
   - `GET /api/profile` 成功返回
   - 收藏/加购分别验证 `POST /api/wishlist`、`POST /api/cart`  
3) 创建商品并上传图片：
   - `POST /api/upload/image` 返回 `/uploads/images/...`
   - 新商品 `image` 字段设置为该 URL，页面能正常加载图片  
4) 管理员登录进入 Admin：
   - Users/Phones/Reviews/Orders/Logs 列表能分页加载
   - Orders 导出 CSV 成功下载（`GET /api/admin/orders/export`，`responseType=blob`）
